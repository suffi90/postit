== PostIt

A Reddit-like application built with Rails.

== Getting Started

1. Check that you have ruby installed by running `ruby -v`
2. Install `bundler` by running `gem install bundler`
3. `cd` into root and run `bundle install --without production`
4. To start the server, run `rails server`
5. Navigate your browser to `localhost:3000`
